/// Reserved for Language Learning OS conversation-engine code (Engine A
/// scenario matching, Engine B local-AI conversation). The current
/// structured-conversation screens live in packages/ui (see
/// features/conversation/) and read directly from packages/core's
/// ContentRepository — split this out as the engine grows in Phase 5/6.
library;
