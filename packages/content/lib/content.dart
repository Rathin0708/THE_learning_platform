/// Reserved for Language Learning OS content-domain code. Content DB
/// access currently lives in packages/core (see
/// data/repositories/sqlite_content_repository.dart) — split out here if
/// the content layer grows large enough to warrant its own package.
library;
